

import { createContext } from "react";
//here context hold all the state 
const noteContext = createContext();

export default noteContext;

//This is the configuration file u can say to use or setup the useContect API ...